# 项目说明文档：基于 PDF 与 PPT 模板的智能演示文稿生成系统

> 本文档说明本项目的系统架构、核心实现、技术创新点，以及与开源 PPTAgent 的差异和复用情况。

---

## 一、系统架构与核心流程

我们将"从 PDF 与 PPT 模板自动生成演示文稿"这一问题，抽象为如下三阶段流水线（Pipeline）：

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  ① 输入解析   │ ──▶ │  ② 模式提取   │ ──▶ │  ③ 内容生成   │
│  Input       │     │  Pattern     │     │  Content     │
│  Parsing     │     │  Induction   │     │  Generation  │
└──────────────┘     └──────────────┘     └──────────────┘
   PDF + PPT          Layout Schema         Final .pptx
```

- **输入解析**：把用户上传的 PDF 解析成带图、表、章节结构的结构化文档；同时把 PPT 模板拆解为「页面图像 + 元素清单 + 图片描述」。
- **模式提取**：把模板中的所有幻灯片归纳为若干「布局类（layout class）」，每一类提取一份 Schema，描述该类布局下需要填什么元素。
- **内容生成**：让 LLM 基于解析后的 PDF 内容生成大纲，按大纲选择布局，再以「编辑命令」的形式驱动 python-pptx 修改模板生成最终 PPT。

这个三阶段框架决定了模块如何协作、数据如何流动，是项目最顶层的设计。

---

## 二、具体实现

### 2.1 输入解析（Input Parsing）

#### 使用的现成工具/库

| 工具/库 | 用途 |
|---|---|
| LibreOffice + `pdf2image` | PPT → PDF → 逐页图像的格式转换 |
| **Marker** | PDF → Markdown，并提取图像、表格元数据 |
| **python-pptx** | 读取 PPT 模板的形状、图片、文本框等元素 |
| 视觉模型 API（演示采用 GPT-4o） | 为 PPT 页面图像和文档图像生成描述 |

#### 自行实现的核心代码与逻辑

**A. 模板分析管道**

| 函数 / 类 | 作用 |
|---|---|
| `ppt_to_images_async` | 将上传的 PPT 逐页转图：先用 LibreOffice 命令行把 PPT 转为 PDF，再用 `pdf2image` 把 PDF 每页渲染为图像 |
| `ImageLabler` | 统一管理 PPT 中的图像信息 |
| `ImageLabler.collect_images` | 遍历每张幻灯片中的 `Picture`，记录图像的尺寸、出现页码、出现次数、相对面积；并把不连续的页码压缩为区间（如 `[1,2,3,5]→[1-3, 5]`），取跨度最长的前 3 段，便于下游展示 |
| `ImageLabler.caption_images_async` | 异步遍历未生成 caption 的图像，调用视觉模型 + `prompts/caption.txt` 为每张图生成不超过 50 词的描述，最后通过 `apply_stats` 写回到 PPT 元素 |

**B. PDF 内容结构化管道（核心）**

整个 PDF 解析流程的入口是 `Document.from_markdown_async`，它把 Marker 输出的 Markdown 文本进一步加工成结构化的 `Document` 对象。

| 步骤 | 函数 | 说明 |
|---|---|---|
| ① 转 Markdown | Marker | 把 PDF 转为 Markdown，并把图像/表格元数据保存下来 |
| ② 标题抽取与重整 | `from_markdown_async` | 用正则 `^#+\s+.*` 抽取所有标题（`#` 表示一级，`##` 表示二级…），调用 LLM（`prompts/heading_extract.txt`）合并冗余标题、修正层级 |
| ③ 知识块切分与合并 | `split_markdown_by_headings` | 用 `edit_distance` 把 LLM 调整后的标题对齐回原始标题，再按行扫描切块；若某个块小于 `min_chunk_size=64` 字符，就向前合并，避免碎片 |
| ④ 段落、表格、图像分离 | `to_paragraphs` | 通过 `MARKDOWN_TABLE_REGEX (\|.*\|)` 和 `MARKDOWN_IMAGE_REGEX (!\[.*\]\(.*\))` 把每个块拆为段落 / 表格 / 图像；为每个媒体附带其前后各 256 字符的「上下文（near_chunks）」 |
| ⑤ 子章节识别 | `_parse_chunk_async` 内部 | 把整个块送给 `doc_extractor` 这个 Agent，让 LLM 输出子章节标题、内容、以及元数据 |
| ⑥ 媒体语义对齐 | `link_medias` | 把表格、图像挂到「语义最相近的子章节」：上下文短的直接挂到首段；上下文长的对所有重写后段落算 `edit_distance`，挑相似度最高的挂入 |
| ⑦ 媒体 caption 生成 | `Media.get_caption_async` / `Table.get_caption_async` | 图像调用视觉模型，表格调用语言模型，结合 `near_chunks` 生成符合上下文的 caption |
| ⑧ 章节摘要 | `SECTION_SUMMARY_PROMPT` | 对完整的当前块生成一段 summary |
| ⑨ 元数据合并 | `MERGE_METADATA_PROMPT` | 把每个块抽到的元数据 dict 用 LLM 合并去重，得到 `Document.metadata`，并自动注入 `presentation-date` |

**容错机制**：`_parse_chunk_async` 内置了最多 3 次的重试。任何一步抛出异常时，会通过 `extractor.retry(error, traceback, ...)` 把错误信息回喂给 LLM，让其基于错误反馈重新生成。

---

### 2.2 模式提取（Pattern Induction）

#### 使用的现成工具/库

| 工具/库 | 用途 |
|---|---|
| Embedding 模型（CLIP / OpenAI Embeddings） | 把幻灯片图像或文本转为向量 |
| LLM API（GPT-4o） | 分析幻灯片功能（封面/目录/内容页）、生成布局描述 |
| `scipy` 层次聚类 | 在余弦相似度矩阵上做 Ward 聚类 |

#### 自行实现的核心代码与逻辑

**核心入口：`SlideInducter.layout_induct`**

该函数把模板里所有页面归纳成若干「布局类」，每一类挑一张代表页作为模板。整体分为三步：

**第一步：功能性 / 内容性自动分类（`category_split`）**

调用 LLM（`prompts/category_split.txt`），把所有幻灯片切成两类：

- **功能性页**：开头页（opening）、目录页（table of contents）、章节大纲页（section outline）、结尾页（ending）—— 不承载具体内容
- **内容性页**：实际承载文档信息的页

LLM 直接返回类似 `{"opening": [1], "table of contents": [2], "section outline": [3, 7], "ending": [12]}` 的结构。

**第二步：内容性页的布局聚类（`layout_split`）**

```
内容性页
    │
    ├── 按 (slide_layout_name, content_type) 预分组
    │
    ├── 对每组内的页面提取图像 embedding（CLIP，768 维）
    │       │
    │       └── images_cosine_similarity → N×N 余弦相似度矩阵
    │
    ├── get_cluster()：Ward 链接的层次聚类，自动确定簇数
    │
    └── 每个簇选「形状最多」的页面作为模板（template_id）
            │
            └── 视觉模型（prompts/ask_category.txt）为模板生成布局名称
```

详细步骤：

1. **图像预处理**：先把 PPT 页面渲染为标准化图像（文本被替换为占位符），避免具体内容干扰布局相似度。
2. **向量提取**：用 CLIP 提取每页 768 维特征向量。
3. **相似度矩阵**：在每个 `(layout_name, content_type)` 子组内构建 N×N 余弦相似度矩阵。
4. **层次聚类**：用 Ward 链接准则做层次聚类，自动确定最优簇数。
5. **代表页选择**：每个簇选 `len(slide.shapes)` 最大的那一页（形状最多，内容最丰富）。
6. **布局命名**：把代表页图像送给视觉模型，让它生成一个语义化的布局名称（如 `"Title and Image:image"`）。

**第三步：内容 Schema 提取（`content_induct`）**

对每一个布局类，把代表页的 HTML 表示送给 `schema_extractor` Agent，让 LLM 输出该布局的内容 Schema：

```json
{
  "title": {
    "description": "the slide title",
    "type": "text",
    "data": ["Original Title Text"]
  },
  "body": {
    "description": "main content bullets",
    "type": "text",
    "data": ["bullet 1", "bullet 2"]
  },
  "figure": {
    "description": "supporting illustration",
    "type": "image",
    "data": ["caption of the picture"]
  }
}
```

通过 `check_schema` 严格验证：每个 text 元素的 data 必须能在幻灯片的 `<p>` 段落中找到对应（edit_distance > 0.8），每个 image 元素必须能在 `<img>` 的 alt 中找到。验证失败则回喂错误给 LLM，最多重试 3 次。

---

### 2.3 内容生成（Content Generation）

#### 使用的现成工具/库

| 工具/库 | 用途 |
|---|---|
| **python-pptx** | 实际修改 PPT：替换文字、克隆段落、替换图片等 |
| LLM API | 生成大纲、生成填充内容、生成编辑命令 |
| Embedding 模型 | 大纲检验阶段做语义匹配 |

#### 自行实现的核心代码与逻辑

**A. 准备阶段：`PPTGen.set_reference`**

把 `SlideInducter` 输出的 `slide_induction` 拆成三组：

- `functional_layouts`：开头/目录/章节大纲/结尾
- `text_layouts`：纯文本布局（key 以 `:text` 结尾）
- `multimodal_layouts`：含图像的布局

并通过 `_hide_small_pics` 隐藏占比小于 20% 的小图（视为装饰元素），降低后续 LLM 干扰。

**B. 主流程：`generate_pres`**

```
generate_pres(source_doc, num_slides)
    │
    ├── generate_outline ───── planner Agent 基于 document overview 输出大纲
    │       │
    │       ├── _fix_outline：双重校验
    │       │       ├── check_retrieve：每条大纲项的 (section, subsection) 必须能在文档里找到
    │       │       └── check_images_async：图像 caption 必须能匹配文档已有图像
    │       │       （任一失败就把错误反馈给 planner，最多 3 次重试）
    │       │
    │       └── _add_functional_layouts：在大纲里插入功能性页
    │               ├── TOC / OPENING / ENDING（固定位置）
    │               └── SECTION_OUTLINE（章节切换时自动插入过渡页）
    │
    └── for each outline_item:
            generate_slide(slide_idx, outline_item)
                │
                ├── 功能性页：edit_distance 模糊匹配最相似的功能模板
                │
                └── 内容性页：
                        ├── _select_layout
                        │       ├── outline_item.retrieve → (header, content_source, images)
                        │       ├── content_organizer Agent 提炼关键要点
                        │       └── layout_selector Agent 在候选 layouts 中选最合适的
                        │
                        ├── _generate_content
                        │       └── editor Agent 输出 editor_output → 转成命令列表
                        │           (元素名, 类型, "quantity_change: N", 旧内容, 新内容)
                        │
                        └── _edit_slide：执行命令并修改 PPT
```

**C. 命令系统：5 个原子操作**

最关键的设计是把所有 PPT 编辑动作收敛为 5 个 API 函数（`apis.py: API_TYPES.Agent`）：

| API | 作用 |
|---|---|
| `replace_paragraph(div_id, paragraph_id, text)` | 替换段落文字 |
| `clone_paragraph(div_id, paragraph_id)` | 克隆段落（用于条目数量增加） |
| `del_paragraph(div_id, paragraph_id)` | 删除段落（用于条目数量减少） |
| `replace_image(img_id, image_path)` | 替换图片，自动按比例适配尺寸 |
| `del_image(figure_id)` | 删除图片 |

`coder` Agent 拿到命令列表后，会输出一段由这 5 个 API 调用组成的代码；`CodeExecutor` 在受限的 `eval` 沙箱里逐行执行。

**D. 自我修正：`_edit_slide`**

```python
for error_idx in range(self.retry_times):           # 默认 retry_times=3
    edit_slide = deepcopy(template_slide)            # 关键：每次都从干净模板开始
    feedback = code_executor.execute_actions(...)
    if feedback is None:                             # 全部成功
        break
    edit_actions = await coder.retry(feedback, ...)  # 把错误反馈给 coder
```

每一次重试都会 **深拷贝原始模板**，保证错误不会累积；`CodeExecutor` 同时维护 `api_history`、`command_history`、`code_history` 三套带状态标记的历史，便于 LLM 准确定位失败原因。

---

## 三、核心技术创新点

### 3.1 智能 PDF 结构化解析算法

PDF 解析采用 **Marker 结构解析 + LLM 语义理解** 的混合策略，相比传统纯结构化方法，具备如下能力：

- **多层级标题结构优化**：通过 LLM 智能调整标题层次，合并冗余标题，修正错误层级。
- **基于语义距离的图表-文本关联**：`link_medias` 用 `edit_distance` 把图表挂到语义最相近的子章节，而不是简单按位置就近。
- **自适应知识块合并**：`split_markdown_by_headings` 根据 `min_chunk_size` 动态合并过小的块，避免碎片化。
- **可重试的解析容错**：`_parse_chunk_async` 失败时把错误回喂给 LLM，最多 3 次。

### 3.2 创新的布局模式提取算法

我们设计了一套针对「PPT 模板」的布局聚类方法，相比直接按 `slide_layout_name` 分组更精细：

- **功能性 vs 内容性自动分类**：`category_split` 通过 LLM 区分两类页，避免功能页污染内容布局。
- **CLIP 向量 + Ward 聚类**：`layout_split` 在标准化后的页面图像上做层次聚类，自动决定布局类别数。
- **代表页智能选择**：每簇选「形状最多」的页面作为模板，因为它信息密度最高、Schema 最完整。
- **Schema 自检与修复**：`check_schema` 严格校验 LLM 输出的 Schema 必须与代表页元素一一对应，否则回喂重试。

### 3.3 自适应内容生成引擎

- **命令驱动编辑系统**：把所有编辑动作收敛为 5 个原子 API（`replace_paragraph` / `clone_paragraph` / `del_paragraph` / `replace_image` / `del_image`），降低 LLM 出错概率。
- **三层重试机制**：planner 大纲 / editor 内容 / coder 代码三个层级，每一层都独立可重试。
- **从干净模板重试**：每次重试都 `deepcopy` 原始模板，避免错误累积。
- **多模态语义验证**：`check_images_async` 用「编辑距离 + 向量余弦相似度」双重匹配，自动替换最相似的图像引用。

---

## 四、代码复用情况与技术差异化说明

### 4.1 与开源 PPTAgent 的关系

本项目以开源项目 **PPTAgent**（`icip-cas/PPTAgent`，EMNLP 2025）的早期 `pptagent/` 子模块为起点进行二次开发。

#### 复用范围

PPT 操作工具模块（`apis.py` 中 5 个底层 API 的接口签名）参考了 PPTAgent 的设计，**这部分基础工具代码约占总代码量的 10%**。

#### 我们做的修改

| 修改点 | 说明 |
|---|---|
| 依赖库适配 | 使用定制的 `pptagent_pptx` 替代标准 `python-pptx`，支持更精细的元素操作 |
| 安全机制增强 | 在 `CodeExecutor.execute_actions` 中以 `eval(line, {}, {func: partial_func})` 限制全局命名空间，并通过 `SAFE_EVAL_GLOBALS` 防止代码注入 |
| 错误处理优化 | 引入 `SlideEditError` 专用异常类、`HistoryMark` 状态枚举、`clone/del` 互斥冲突检测，更好地服务自我修正机制 |
| 接口异步化 | 在 polyfill 兼容 Python 3.10 的前提下保留同步与异步双路径 |

### 4.2 与开源 PPTAgent 的核心差异

| 维度 | 开源 PPTAgent | 我们的项目 |
|---|---|---|
| **技术路线** | Agent-based 通用生成框架，输入源较为多样 | 专门针对 PDF→PPT 场景的三阶段流水线（输入解析→模式提取→内容生成）|
| **核心算法** | 通用文档解析与布局推荐 | 深度定制的 PDF 结构化解析 + 创新布局聚类（功能/内容自动分类 + CLIP 向量聚类）|
| **质量保证** | 主要依赖 PPTEval 等外部评估框架 | 内置自我修正机制 + 多层语义验证（语义匹配、结构校验、格式检查）|
| **应用场景** | 面向通用 PPT 生成需求 | 专业化的学术 / 商务 PDF→PPT 解决方案 |

### 4.3 核心创新贡献

1. **算法架构创新**：将 LLM 的角色从「内容生成者」升级为「内容操作 Agent」，基于模板复用做智能化的细粒度编辑。
2. **自我修正机制**：实现多层重试 + 智能错误恢复 + 干净状态重启，从机制上保证生成质量的稳定性与可靠性。

虽然底层工具借鉴了开源方案，但本项目的核心价值在于针对特定场景的算法创新与系统重构，实现了从「通用化生成」到「专业化编辑操作」的优化。

---

## 五、其他问题补充：关于自我修正机制

> **问题背景**：演示视频中的 PPT 与现场展示的 PPT 不完全一致，视频中存在表格内容缺失。两者输入完全相同（同一份 PPT 模板 + 同一份 PDF），输出差异源于 LLM 调用的随机性。

针对这类问题，我们设计了一套较为完备的自我修正机制：

### 5.1 三层重试架构

| 层级 | 实现 |
|---|---|
| **系统级** | 默认 `retry_times=3` 的全局配置，所有组件统一遵守 |
| **Agent 级** | LLM 输出错误时，把错误信息 + traceback 重新打包成 prompt 回喂，并保留对话历史让 LLM 理解上下文 |
| **执行级** | 每一行 API 调用包裹在 try/catch 中，出错立即返回详细信息供上层重试 |

### 5.2 内容缺失处理

- **大纲完整性验证**：`_fix_outline` 检查每个大纲项是否能在源文档中找到对应章节/子章节，失败则把缺失信息反馈给 planner Agent 重新生成，递归直至成功或耗尽重试次数。
- **相似度阈值（`sim_bound`）**：`edit_distance` 低于阈值（默认 0.5）即视为内容缺失或不准确。

### 5.3 图表不完整 / 格式错乱处理

- **双重图像匹配**：`check_images_async` 先用 `edit_distance` 比较 caption 文本相似度，不足时再用语义向量的余弦相似度做精确匹配，自动替换不匹配的引用。
- **从干净模板重试**：`_edit_slide` 每次重试都 `deepcopy(self.presentation.slides[template_id-1])`，避免错误状态累积；失败副本直接丢弃。

### 5.4 API 调用错误处理

- **预防性检查**：执行前校验函数名是否注册、参数格式是否正确、是否存在逻辑冲突。
- **专用异常**：`SlideEditError` 专门标识 PPT 编辑错误（元素不存在、类型不匹配、操作冲突等），并提供具体描述。
- **冲突检测**：在 `execute_actions` 中跟踪 `command_history`，禁止同一命令内同时出现 `clone_*` 与 `del_*`。

### 5.5 智能错误反馈循环

- **三套历史管理**：`api_history`、`command_history`、`code_history` 各自带 `HistoryMark` 状态标记（`API_CALL_ERROR` / `CODE_RUN_CORRECT` 等）。
- **错误格式化报告**：失败时生成「失败具体行 + 错误类型 + traceback」的报告，规范化送给 LLM。
- **自适应调整**：LLM 根据错误类型调整策略：元素 ID 错误→重查 ID；操作冲突→调整操作序列。

### 5.6 局限性分析与改进方向

即便有较完备的自我修正机制，**表格内容仍可能缺失**，原因如下：

1. **数值精度问题**：LLM 可以理解表格语义，但对高精度数值数据容易出现错乱、丢失，且每次输出难以保证一致。
2. **`python-pptx` 限制**：处理表格时无法直接验证表格数据的完整性。
3. **多轮重试中的上下文衰减**：表头、数据来源、计算逻辑等在多轮重试中可能逐渐丢失，导致重试结果与原始需求偏差越来越大。

**后续改进方向**：
- 为表格数据单独设计专用验证机制（结构化对比 + 数值精度检查）。
- 在 PDF 解析阶段就把表格以结构化形式（cells + merge_area）保存，避免后续转换中信息丢失。
- 重试时显式保留表格上下文，防止衰减。

由于课程时间限制，这部分工作有待进一步完善。

---

## 六、组内分工

| 成员 | 主要职责 |
|---|---|
| **刘洋** | 项目调研与整体架构设计；完成 PPT 模板分析与布局聚类算法设计；负责课程汇报材料制作与项目展示答辩 |
| **石佳蓉** | 核心算法实现：PDF 解析、自我修正机制、Agent 编辑操作、多模态内容处理；主要代码开发与算法优化 |
| **刘乾** | 系统集成与用户界面开发；前后端联调与 API 接口实现；演示 demo 制作、功能测试与部署配置 |
