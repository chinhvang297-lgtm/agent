# 项目对比说明文档

> 本文档说明我们的课程项目与开源项目 **PPTAgent** 之间的关系、范围差异、以及我们在文档解析与前端层面所做的修改。

---

## 1. 项目来源声明

- 上游开源项目:**PPTAgent**(`https://github.com/icip-cas/PPTAgent`)。
- 我们项目所基于的是 PPTAgent 的较早期版本(对应 `pptagent` 包,在我们仓库中保留为 `pptagent/` 目录)。
- 上游项目仍在持续演进,目前已发展为一个由两条代码路径组成的较大系统:
  - `deeppresenter/`:新一代产品层(Typer CLI、Agent 编排、HTML→PPTX 流水线、MCP 工具集、Docker 部署等)。
  - `pptagent/`:早期核心生成与评估库(布局归纳、文档解析、模板驱动 PPT 生成、评估)。
- 我们项目**只使用了上游 `pptagent/` 这一层**,且使用的是这一层的早期快照。我们在此基础上对若干文件做了修改,并改写了前端页面。

---

## 2. 我们项目在上游全栈中的位置

下图给出上游 PPTAgent 当前的整体结构,以及我们项目所覆盖的范围(打 ✅ 表示我们项目里有,❌ 表示没有):

```
PPTAgent (上游当前版)
├── deeppresenter/                ❌ 我们没有使用
│   ├── cli/                      ❌  (Typer CLI: onboard / generate / serve / config / clean)
│   ├── agents/                   ❌  (Research / PPTAgent / Design 三类 Agent)
│   ├── tools/                    ❌  (MCP 工具:搜索、研究、反思、文件转换、任务管理)
│   ├── utils/                    ❌  (配置、日志、MinerU、Web 转换、MCP 客户端)
│   ├── html2pptx/                ❌  (Node 端 HTML→PPTX 转换)
│   └── test/                     ❌
│
├── pptagent/                     ✅ 我们以此为起点
│   ├── agent.py                  ✅ (有改动)
│   ├── apis.py                   ✅
│   ├── llms.py                   ✅
│   ├── induct.py                 ✅ (布局归纳)
│   ├── pptgen.py                 ✅ (PPT 生成)
│   ├── ppteval.py                ✅ (评估)
│   ├── multimodal.py             ✅
│   ├── model_utils.py            ✅
│   ├── utils.py                  ✅
│   ├── document/                 ✅ (有改动 — 见第 4 节)
│   ├── presentation/             ✅
│   ├── prompts/                  ✅ (扁平结构;上游已细分为 prompts/document、prompts/ppteval)
│   ├── roles/                    ✅
│   ├── resource/                 ✅ (与上游完全一致,未改)
│   ├── mcp_server.py             ❌ (上游 MCP 服务入口,我们没有使用)
│   ├── docker/                   ❌
│   ├── scripts/                  ❌
│   ├── templates/                ❌
│   └── test/                     ❌
│
├── webui.py                      ❌ (上游官方 webui;我们前端是自己重写的)
├── docker-compose.yml            ❌
├── pyproject.toml                ❌ (上游打包元数据)
└── README.md / DOC.md / ...      ❌
```

**结论:我们使用的范围是上游 `pptagent/` 这一个子目录的早期版本,且不包含其中的 `mcp_server.py`、`docker/`、`scripts/`、`templates/`、`test/` 等工程化部分。上游 `deeppresenter/` 整层我们完全没有触碰。**

---

## 3. 包与模块层面的整体对比

| 维度 | 上游(当前版) | 我们项目 |
|---|---|---|
| 包版本号 (`__init__.py`) | `1.0.2` | `0.1.0` (早期) |
| Python 兼容 | 直接使用 `asyncio.TaskGroup`(要求 3.11+) | 自带 polyfill,兼容 Python 3.10 |
| 包导出风格 | 显式 `__all__` 暴露 `Document`、`PPTAgent`、`Presentation` 等高层类 | 通配 `from .x import *`,导出粒度更粗 |
| 工程化 | 含 `pyproject.toml`、Docker、CLI、MCP server、单元测试 | 只保留核心库代码,无工程化外壳 |
| Prompts 组织 | 已细分 `prompts/document/`、`prompts/ppteval/` 子目录 | 全部扁平放在 `prompts/` 下 |
| 资源文件 (`resource/`) | — | **与上游完全一致(目录 SHA 相同)**,未做修改 |

---

## 4. 文档解析模块的逻辑对比(重点)

文档解析是我们改动最集中的模块之一,这一节聚焦 `pptagent/document/document.py` 与 `pptagent/document/element.py` 的逻辑差异。

### 4.1 数据建模方式不同

| | 我们这一版(早期) | 上游当前版 |
|---|---|---|
| 基类 | `@dataclass` | `pydantic.BaseModel` |
| 反序列化 | 手写 `from_dict` 类方法,逐字段断言 | 由 pydantic 模型校验,可直接接入 LLM `response_format` |
| 校验 | 在 `__post_init__` / `validate_medias` 中手动校验 | 由 pydantic 校验器和类型系统保证 |

> 体现:我们这一版把"和 LLM 的接口契约"写成了 prompt + 手工 dict 解析;上游已经将 pydantic 模型当作 schema,直接驱动 LLM 的结构化输出。

### 4.2 章节内的层级结构不同

| | 我们这一版 | 上游当前版 |
|---|---|---|
| `Section` 内字段 | `subsections: list[SubSection]` | `content: list[SubSection \| Media \| Table]` |
| 媒体的位置 | 嵌在 `SubSection.medias` 里(图片/表格属于子章节) | 与 `SubSection` 同级,直接出现在 `content` 中 |
| 索引方式 | 通过 `Document[sec_key][subsec_key]` 二级索引 | `Document` 直接可迭代 `(section, content)` 对,内容块平铺 |

> 体现:两边对"一个章节里的图、表、文字"的建模方式不同。我们这一版认为图/表从属于段落;上游把它们提到与段落对等的位置,允许图、表、段落任意顺序穿插。

### 4.3 章节切分策略

| | 我们这一版 | 上游当前版 |
|---|---|---|
| 入口函数 | 同步 `split_markdown_by_headings` | 异步 `split_markdown_by_headings` |
| 切分依据 | 直接调用 LLM 给出 adjusted headings,然后用 `edit_distance` 把"调整后的标题"对齐回原始标题 | 先用 `count_markdown_chunks` + `calculate_hierarchical_counts` 构建带字数统计的文档树,再把树喂给 LLM 选出"逻辑标题",最后切分 |
| 最小块阈值 | `min_chunk_size = 64`,小块向前合并 | 环境变量 `MIN_CHUNK_SIZE`(默认 512),小块向前合并 |
| 最大块处理 | 无限制 | 超过 `MAX_CHUNK_SIZE`(默认 32768) 时,递归在中点附近的 heading 处二分(`split_large_chunks`) |

> 体现:上游对长文档的稳健性更高(有最大块上限并能递归切分),并且引入了"先看文档树整体再决定切点"的两阶段策略。我们这一版是单阶段、无最大块保护的。

### 4.4 同步/异步实现

- 我们这一版**同时维护同步和异步两条路径**:`_parse_chunk` 与 `_parse_chunk_async`、`from_markdown` 与 `from_markdown_async`、`Media.parse` 与 `Media.parse_async`、`get_caption` 与 `get_caption_async`,代码量翻倍。
- 上游**只保留异步路径**,代码更精简。

### 4.5 表格解析

| | 我们这一版 | 上游当前版 |
|---|---|---|
| 入口 | `Table.parse_table` | `parse_table_with_merges` |
| 思路 | 用 `mistune` 把 markdown 转成 HTML,再用 BeautifulSoup 抓 `<table>`;并处理"行被 `\n` 内嵌拆开"的边角 | 直接处理 HTML 表格,通过 `rowspan`/`colspan` 网格化,产出 `cells` + `merges` 区域 |
| 是否再走一次 LLM | 是,会调用 `table_model` + `prompts/table_parsing.txt` 让模型重新整理表格内容 | 否,纯结构化解析 |
| 渲染表格图片 | `markdown_table_to_image` | `get_html_table_image` |

### 4.6 媒体与段落的关联(`link_medias`)

| | 我们这一版 | 上游当前版 |
|---|---|---|
| 输入/输出 | 接受"重写后段落"列表(dict),把媒体追加到匹配段落的 `medias` 字段中 | 接受 `Section` 对象,直接把 `Media` 实例 `insert` 到 `section.content` 列表中 |
| 匹配方式 | 上下文短:挂到首段落;上下文长:用 `edit_distance` 与各段落 `markdown_content` 做对比 | 上下文短:`insert(0, media)`;上下文长:与各 `SubSection.content` 比 `edit_distance`,插入到最相似 SubSection 之后 |

### 4.7 失败重试

- 我们这一版:`_parse_chunk` 在异常时会调用 `extractor.retry(error, traceback, turn_id, retry+1)` 把错误反馈给 LLM,最多重试 3 次。
- 上游:无显式重试,异常直接抛出。

### 4.8 元数据合并

- 我们这一版:从每个块中弹出 `metadata`(任意 dict),最后用 LLM 合并成 dict 直接当作 `Document.metadata`;`Document.__post_init__` 会自动注入 `presentation-date`(当天日期)。
- 上游:用 pydantic `Metadata{name, value}` 模型约束,合并阶段强制返回 `list[Metadata]`,再转成 dict;不再注入 presentation-date。

### 4.9 依赖与外部库

| | 我们这一版 | 上游当前版 |
|---|---|---|
| `element.py` 依赖 | `mistune` + `bs4` + `PIL` + `torch`(用于 `cosine_similarity` 做图像匹配) | `PIL` + `pydantic`(更轻量) |
| 文档侧 OutlineItem | 我们 `document.py` 末尾包含 `OutlineItem` 类(包含 `retrieve` / `check_retrieve` / `check_images` / `check_images_async`,用 `torch.cosine_similarity` 做图像 caption 匹配) | 上游已将该类移出 `document/`,不再依赖 torch |

> 体现:我们这一版的运行时依赖更重(尤其需要装 `torch`、`mistune`、`bs4`),上游已经做了瘦身。

---

## 5. 我们在上游基础上做的修改

> 这一节是说明老师"我们到底动了什么"的核心。请将下面占位符替换为真实修改内容,涉及到具体函数、文件、效果时尽量写到位。

### 5.1 解析逻辑层面

- **[待补充]** 你们改过的解析相关函数 / 文件。建议格式:
  - 文件路径: `pptagent/document/xxx.py`
  - 改动点: 具体函数名 / 行为变化
  - 改动原因: 为什么这么改(例如:对中文文档不友好、上游策略对我们的输入数据效果差、原版报错等)
  - 改动效果: before / after 用一个例子说明

### 5.2 前端页面层面

> 上游的 `webui.py` 我们没有使用,前端是我们重写的。

- **[待补充]** 前端使用的技术栈(框架 / 语言)
- **[待补充]** 实现了哪些页面 / 路由
- **[待补充]** 与原版 `webui.py` 的差异,例如交互流程、页面布局、与后端的接口
- **[待补充]** 截图(before / after,或仅 after)

### 5.3 其它(如有)

- **[待补充]** 配置、prompt、role yaml 的修改
- **[待补充]** 新增的脚本 / 工具

---

## 6. 我们当前版本的能力边界与已知不足

为了不夸大我们的工作,这一节如实说明我们项目目前能做什么、哪些上游能力我们没有覆盖。

### 6.1 当前可以跑通的最小流程

- **[待补充]** 输入是什么(例如:一份 markdown + 一组图片 + 一份 PPT 模板)
- **[待补充]** 输出是什么(例如:一份 .pptx 文件)
- **[待补充]** 跑通需要的环境(Python 版本、关键依赖、API key 等)

### 6.2 相对上游缺失的能力

- 没有上游 `deeppresenter/` 提供的 Research/Design 多 agent 协作流程。
- 没有 MCP server (`mcp_server.py`),即不能作为 MCP 工具被外部调用。
- 没有 Docker 部署、CLI 命令封装、HTML→PPTX 的 Node 转换链。
- 没有上游 `prompts/document/` 与 `prompts/ppteval/` 子目录的最新提示词版本(我们用的是早期扁平版本)。

### 6.3 我们已识别但暂未修复的问题

- **[待补充]** 例如:长文档的章节切分容易过长(因为没有 MAX_CHUNK_SIZE 保护)
- **[待补充]** 例如:同步路径已用不到但未删,代码冗余
- **[待补充]** 例如:`document_back.py` / `multimodal.py.backup` 这类备份文件应清理

---

## 7. 后续可继续推进的方向

- **[待补充]** 例如:跟上上游的 pydantic 化重构,移除手写 from_dict 与同步代码
- **[待补充]** 例如:引入 `MAX_CHUNK_SIZE` 与递归二分切块,提升对长文档的稳健性
- **[待补充]** 例如:把 OutlineItem 的图像匹配从 torch 余弦改成更轻量的 embedding 方案
- **[待补充]** 例如:前端补充 [具体页面]、改进 [具体交互]

---

## 附录 A:与上游完全一致(未修改)的文件清单

> 以下文件 / 目录在我们项目和上游 `pptagent/` 中**字节级一致**(git blob SHA 相同):

- 整个 `pptagent/resource/` 目录
- `pptagent/prompts/ask_category.txt`
- `pptagent/prompts/caption.txt`
- `pptagent/roles/agent.yaml`
- `pptagent/roles/content_organizer.yaml`
- `pptagent/roles/layout_selector.yaml`

我们如实声明:这些文件直接复用上游,未做修改。

## 附录 B:文件归属

> 上游 PPTAgent 采用其原始 LICENSE。我们在仓库根目录保留了上游 LICENSE 副本,并在 README 中声明了项目来源。
